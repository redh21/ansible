---
- name: Create file1
  copy:
    dest: /home/vm/folder1/file1.txt
    content:
      Text line1, in file1
      Text line2, in file1
      Text line3, in {{ text }}

- name: Create file2
  copy:
    dest: /home/vm/folder2/file2.txt                       
    content:
      Text line1, in file2
      Text line2, in file2
      Text line3, in {{ text }}
